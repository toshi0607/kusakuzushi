{
  "name": "@kusakuzushi/extension",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc && node build.mjs",
    "dev": "node build.mjs --watch",
    "package": "pnpm build && node package.mjs",
    "test": "vitest run"
  },
  "dependencies": {
    "@kusakuzushi/core": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "jsdom": "^30.1.1",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}

# 草崩し ビジュアルデザイン仕様

対象: apps/web(+ packages/core の renderer 描画品質)。拡張版はスコープ外(本物の GitHub ページに溶け込むのが正なので、独自デザインを持ち込まない)。

## 0. コンセプト — 「夜の畑のアーケード」

草崩しは「GitHub の草グラフ」と「ブロック崩し」の掛け算のゲーム。デザインもその掛け算で作る。

- **草グラフは本物のピクセルグリッド**である。草グラフにドット絵を描く開発者文化が実在する(gitfiti 等)。だからピクセル表現は「レトロ風の衣装」ではなく題材そのもの
- **ブロック崩しはアーケードゲーム**である。アーケードの文法(アトラクトモード、スコア表示、PRESS START)を借りる

一方で、AI 生成デザインの定番「真っ黒背景 + 蛍光緑アクセント」に落ちないよう、次の一線を引く。

### 最重要ルール: 緑は草だけのもの

緑(GitHub の5段階スケール)は**コンテンツ(草)専用**。ボタン・リンク・フォーカスリング・スコアなど UI の操作系には一切使わない。UI アクセントは**マーキーアンバー**(アーケード筐体の電飾・スコア LED の色)一色。これにより:

- 画面上で「緑のものは全部、刈り取り対象の草」という一貫した意味論ができる
- 「黒 + 蛍光緑」テンプレから構造的に離れられる

### シグネチャー(この1点に賭ける): アトラクトモード

トップページのヒーローは入力フォームではなく、**動いているゲームそのもの**。合成グリッドに 3x5 ピクセル文字で「KUSAKUZUSHI」を草として描き、AI 操作のパドルが延々とデモプレイしている(アーケードの客寄せ画面 = attract mode)。ユーザー名を入れて開始すると、デモの草が**あなたの本物の草**に置き換わる。

- 「自分の草がこうなる」を言葉ゼロで伝える
- 草グラフにドット絵を描く文化への目配せ(タイトルロゴが草でできている)
- 実装コストは低い: 合成 ContributionGrid はただのデータ、autopilot はパドル追従数行。core 変更ほぼ不要

シグネチャーはここだけ。他の装飾(スキャンライン、CRT 湾曲、グロー等)は**すべて禁止**。ピクセルはコンテンツに実在するので、フィルタで演出する必要がない。

## 1. カラートークン

草の緑スケールは core の `LIGHT_THEME` / `DARK_THEME`(GitHub 準拠)を不変のコンテンツカラーとして維持。以下はページ側トークン。ダークは「夜の畑」= 青黒ではなく**緑がかった土の黒**を使う(#0d1117 の丸コピー禁止)。

| トークン | ダーク(夜の畑) | ライト(昼の畑) | 用途 |
|---|---|---|---|
| `--soil` | `#0C110D` | `#F4F8F2` | ページ背景 |
| `--field` | `#151C16` | `#FFFFFF` | パネル・入力欄背景 |
| `--ridge` | `#28332A` | `#D8E2D5` | 罫線・枠 |
| `--ink` | `#E4EDE2` | `#1C231B` | 本文 |
| `--ink-faint` | `#8DA08C` | `#5C6B59` | 補足テキスト |
| `--marquee` | `#FFB224` | `#FFB224` | CTA ボタン背景・フォーカスリング・ボール |
| `--marquee-ink` | `#1A1206` | `#1A1206` | アンバー上の文字 |
| `--alert` | `#FF8F8F` | `#B3261E` | エラーメッセージ |

- CTA ボタンは両モードで同一(アンバー地 + 墨文字)。ブランドの錨として明暗で変えない
- ライト/ダークは現行どおり `prefers-color-scheme` 追従を維持(canvas 内テーマ連動も現行実装を活かす)
- コントラスト: `#E4EDE2`/`#0C110D` ≈ 15:1、`#1A1206`/`#FFB224` ≈ 10:1、`#8DA08C`/`#0C110D` ≈ 6.8:1 — いずれも AA 以上

## 2. タイポグラフィ

| ロール | 書体 | 用途 |
|---|---|---|
| ディスプレイ | **DotGothic16**(Google Fonts) | タイトル「草崩し」、リザルト見出し、発射ガイド、CTA ボタン、スコア数字 |
| 本文 | **IBM Plex Sans JP** 400/600 | サブタイトル、入力欄、エラー、統計ラベル、フッター |

- DotGothic16 は日本語ピクセルフォント。かな漢字のドット表示ができる唯一級の Web フォントで、題材(ピクセルグリッド)と直結する
- 読ませる文章には絶対に使わない(1〜2 語の見出し・数字・掛け声のみ)。長文可読性は Plex が担う
- ロード: Google Fonts、`preconnect` + `display=swap`。DotGothic16 は使用グリフが有限(タイトル・見出し・数字・記号)なので `text=` パラメータでサブセット化し数 KB に抑える
- canvas 内 HUD でも DotGothic16 を使うため、セッション開始前に `document.fonts.load()` で確実にロード(未ロード時は sans-serif フォールバックで描画が崩れないこと)

### タイプスケール

- タイトル: `clamp(2rem, 6vw, 3rem)` DotGothic16、字間 `0.08em`
- リザルト見出し: `1.75rem` DotGothic16
- 本文: `0.9375rem / 1.7` Plex 400
- 統計数字: `1.5rem` DotGothic16(ラベルは Plex 0.75rem・`--ink-faint`)

## 3. レイアウト

コンテナ `max-width: 1040px`。1 カラム。ヘッダー/フッターは最小限。

### トップ(アトラクト画面)

```
┌────────────────────────────────────────┐
│              草崩し                      │  ← DotGothic16
│   GitHub の草を、ブロック崩しで刈り取ろう      │  ← Plex, --ink-faint
│                                        │
│ ┌────────────────────────────────────┐ │
│ │ ░▓░ K U S A K U Z U S H I ░▓░      │ │  ← 合成グリッド(3x5ピクセル文字)
│ │ ░ ░░  ▓░   ░▓  ░░  ░  ░░░  ░       │ │     + まばらな level1 ノイズ
│ │              ●                      │ │  ← autopilot ボール(アンバー)
│ │           ▬▬▬▬▬▬                   │ │
│ └────────────────────────────────────┘ │
│                                        │
│   [ GitHub ユーザー名        ] [草を刈る] │  ← 入力 + アンバーCTA
│                                        │
│      ブロック = あなたの1年分の草           │  ← 一行説明
├────────────────────────────────────────┤
│  toshi0607 / GitHub / Chrome拡張(準備中) │  ← フッター小
└────────────────────────────────────────┘
```

- デモ canvas は入力を受け付けない(`pointer-events: none`)。プレイ可能に見えて触れない誤解を防ぐため、盤面下端に小さく `DEMO PLAY` を DotGothic16 で表示
- フォーム送信 → デモを破棄し、同じ場所で本物のセッションに差し替え(レイアウトシフトなし: canvas は同寸)

### プレイ画面

- 上部に一行ステータス: `@{username} ─ {total} contributions`(Plex、小さく)
- canvas(アスペクト比 2:1、`width: 100%`)。枠線は `--ridge` 1px のみ。角丸 8px 維持
- HUD は canvas 内描画(§5)。DOM 側に重複表示しない
- 発射ガイド「クリック / Space で発射」は**盤面下半分(パドル空間)の中央**に置く。草は上半分を占める(core の `brickAreaHeight = canvasHeight/2 - gap`)ため、盤面全体の中央に置くと草の最下行に重なる

### リザルト(収穫レポート)

オーバーレイパネル(`--field` 背景 92% + 8px 角丸 + `--ridge` 枠):

```
┌──────────────────────────┐
│      完全刈り取り!         │  ← DotGothic16
│                          │
│   スコア        刈り取り率   │  ← ラベル: Plex faint
│   12,340         87%     │  ← 数字: DotGothic16 1.5rem
│   ▓▓▓▓▓▓▓▓▓▓▓▓░░░        │  ← 刈り取り率バー: 草セル並び
│                          │
│  [Xで共有] [画像を保存] [もう一回] │
└──────────────────────────┘
```

- 刈り取り率バーは「小さい草セル(10px 角丸 2px)の横並び」で描く。プログレスバーですらグリッドの文法で語る
- 主ボタン「Xで共有」のみアンバー、他 2 つは `--field` 地 + `--ridge` 枠のセカンダリ

### レスポンシブ

- `<560px`: フォーム縦積み(入力とボタンが全幅)、タイトル縮小。リザルトの統計は**横並びのままサイズ縮小**(canvas 高 ~170px に収めるため。縦並び案は高さ超過するので不採用 — 2026-07-25 レビュー M3 で確定)。オーバーレイは padding + overflow-y: auto を安全弁に持つ
- canvas は全幅縮小のみ(内部解像度 960x480 は不変、現行実装どおり)

## 4. モーション

演出は**ページロードの 1 シーケンスに集中**、以外はマイクロのみ。

1. **草の生育**(ロード時 / セッション開始時): グリッドのセルが左の週から右へ列単位でスケールイン(列あたり 12ms ずらし、全体 ~700ms、`ease-out`)。GitHub のグラフ読込アニメの記憶とつながる。実装は renderer に `reveal: 0..1` 引数を追加し列ごとにゲート
2. 生育完了後、デモのボールが落ちてきて autopilot 開始
3. マイクロ: ボタン hover(輝度 +6%、120ms)、リザルトパネルは 200ms のフェード + 8px スライドアップ。以上

`prefers-reduced-motion: reduce` 時: 生育アニメは即時全表示、アトラクトのボールは動かさない(静的な草グリッドのみ)、パネルはフェードなし。

## 5. canvas 描画の磨き込み(packages/core/renderer.ts)

現状は矩形ベタ塗り + 16px sans-serif で「グラフに見えない」のが未洗練さの主因。

| 項目 | 現状 | 変更 |
|---|---|---|
| ブロック形状 | 直角矩形 | 角丸(セル幅の ~20%、GitHub の 2px 相当)。これだけで一気に「草グラフ」に見える |
| ボール | テキスト色の円 | `--marquee` アンバーの円。緑の海の中で唯一の暖色 = 視線誘導 |
| パドル | 矩形 | 角丸カプセル、`--ink` 系 |
| HUD 文字 | `16px sans-serif` | `16px DotGothic16`(Theme に `hudFont?: string` を追加、未指定時は現行フォールバック) |
| ライフ表示 | `Life: 2` テキスト | 小さな草セル(level4 色の角丸正方形)を残機数ぶん並べる |
| パーティクル | 現行(緑の欠片) | 維持。数・寿命は現行のまま(足し算しない) |

Theme 型の変更は optional フィールドのみ(`hudFont?`, `accentColor?`)。拡張版・既存テストへの後方互換を壊さない。

## 6. コピー(文言)

| 箇所 | 文言 |
|---|---|
| タイトル | 草崩し |
| サブタイトル | GitHub の草を、ブロック崩しで刈り取ろう |
| 入力 placeholder | GitHub ユーザー名 |
| CTA | 草を刈る |
| ローディング | {user} の草を取得中… |
| 発射ガイド | クリック / Space で発射 |
| クリア見出し | 完全刈り取り! |
| ゲームオーバー見出し | ゲームオーバー |
| 統計ラベル | スコア / 刈り取り率 |
| 草ゼロ | 崩す草がありません🌵(現行維持) |
| エラー | 現行維持(ユーザー不在 / 取得失敗) |
| デモ表示 | DEMO PLAY |

方針: 操作を言い切る動詞(「草を刈る」= このプロダクトの中心動詞)。飾り言葉なし。絵文字は既存の 🌱🌵 の 2 つ以外増やさない。

## 7. アトラクトモード仕様(apps/web)

- `demo-grid.ts`: `buildDemoGrid(): ContributionGrid`。53x7 のグリッドに 3x5 ピクセル文字で `KUSAKUZUSHI`(11 文字 × 4 列 = 43 列、上下 1 行マージン)を level 3〜4 で描き、残りセルに疎な level 1 ノイズ(密度 ~12%)を撒く
- autopilot: 専用モジュール(attract.ts)。パドル目標 x = ボール x + `18 * sin(t)`(完璧すぎない追従)、最大速度は人間相当にクランプ。ballLost → 1.2 秒後に自動再発射。gameOver / clear → グリッド再生成して最初から(**ライフは core の機構のまま**。ライフ無限案は core 無変更の制約を優先して不採用 — アーケードのデモが定期的に頭へ戻るのは自然な挙動。2026-07-25 レビュー L3 で確定)
- HUD はデモ中非表示(スコアの意味がないため)。`DEMO PLAY` のみ右下に表示
- フォーム送信時に demo セッションを destroy → 実セッションを同一コンテナへ

## 8. 共有画像(リザルト保存)の体裁

現状はゲーム canvas の生スクショ。共有される絵がプロダクトの顔になるため、オフスクリーン canvas 1200x630(OGP 比)に合成する:

- 背景 `--soil`、最終盤面のスナップショット(角丸クリップ)、`@{username}`、スコア/刈り取り率(DotGothic16)、右下にワードマーク「草崩し」
- X intent のテキストは現行仕様を維持

## 9. 品質フロア(全フェーズ共通の完了条件)

- モバイル(375px)〜デスクトップで破綻なし
- フォーカスリング: `outline: 2px solid var(--marquee); outline-offset: 2px` を全操作要素に
- `prefers-reduced-motion` 尊重(§4)
- ライト/ダーク両モードでスクリーンショット確認
- Lighthouse で FOUT/CLS の劣化なし(フォント導入後に確認)

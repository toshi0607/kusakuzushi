# 草崩し ビジュアルデザイン仕様

対象: apps/web(+ packages/core の renderer 描画品質)。拡張版は原則スコープ外(本物の GitHub ページに溶け込むのが正なので、独自デザインを持ち込まない)。

**例外(2026-07-27 決定): 玉の色 `--marquee` と玉・パドルの形は拡張にも持ち込む。** パドル・HUD・リザルトバナーは
これまでどおりページから採った色を使い、GitHub のテーマ(ライト/ダーク、アカウント設定)に追従する。玉だけを例外に
した理由は 2 つ。

- multiBall で玉が十数個に増えると、ページの文字色一色ではクリア後の薄い草の上に**黒い粒がばら撒かれた**ようにしか
  見えない。アンバーなら明暗どちらの GitHub でも玉として読める
- 玉とパドルは web と拡張で唯一まったく同じ形・同じ物理で動く。ここが揃うと「同じゲーム」だと分かる

色の実体は `MARQUEE_COLOR`(packages/core/src/renderer.ts)一箇所に置き、web / 拡張 / 共有画像がそこを参照する。

## 0. コンセプト — 「夜の畑のアーケード」

草崩しは「GitHub の草グラフ」と「ブロック崩し」の掛け算のゲーム。デザインもその掛け算で作る。

- **草グラフは本物のピクセルグリッド**である。草グラフにドット絵を描く開発者文化が実在する(gitfiti 等)。だからピクセル表現は「レトロ風の衣装」ではなく題材そのもの
- **ブロック崩しはアーケードゲーム**である。アーケードの文法(アトラクトモード、スコア表示、PRESS START)を借りる

一方で、AI 生成デザインの定番「真っ黒背景 + 蛍光緑アクセント」に落ちないよう、次の一線を引く。

### 最重要ルール: 緑は草だけのもの(HUD も例外ではない)

緑(GitHub の5段階スケール)は**コンテンツ(草)専用**。ボタン・リンク・フォーカスリング・スコアなど UI の操作系には一切使わない。UI アクセントは**マーキーアンバー**(アーケード筐体の電飾・スコア LED の色)一色。これにより:

- 画面上で「緑のものは全部、刈り取り対象の草」という一貫した意味論ができる
- 「黒 + 蛍光緑」テンプレから構造的に離れられる

### シグネチャー(この1点に賭ける): アトラクトモード

トップページのヒーローは入力フォームではなく、**動いているゲームそのもの**。合成グリッドに 3x5 ピクセル文字で「KUSAKUZUSHI」を草として描き、AI 操作のパドルが延々とデモプレイしている(アーケードの客寄せ画面 = attract mode)。ユーザー名を入れて開始すると、デモの草が**あなたの本物の草**に置き換わる。

- 「自分の草がこうなる」を言葉ゼロで伝える
- 草グラフにドット絵を描く文化への目配せ(タイトルロゴが草でできている)
- 実装コストは低い: 合成 ContributionGrid はただのデータ、autopilot はパドル追従数行。core 変更ほぼ不要

シグネチャーはここだけ。他の装飾(スキャンライン、CRT 湾曲、グロー等)は**すべて禁止**。ピクセルはコンテンツに実在するので、フィルタで演出する必要がない。

## 1. カラートークン

草の緑スケールは core の `LIGHT_THEME` / `DARK_THEME`(GitHub 準拠)を不変のコンテンツカラーとして維持。以下はページ側トークン。ダークは「夜の畑」= 青黒ではなく**緑がかった土の黒**を使う(#0d1117 の丸コピー禁止)。

| トークン | ダーク(夜の畑) | ライト(昼の畑) | 用途 |
|---|---|---|---|
| `--soil` | `#0C110D` | `#F4F8F2` | ページ背景 |
| `--field` | `#151C16` | `#FFFFFF` | パネル・入力欄背景 |
| `--ridge` | `#28332A` | `#D8E2D5` | 罫線・枠 |
| `--ink` | `#E4EDE2` | `#1C231B` | 本文 |
| `--ink-faint` | `#8DA08C` | `#5C6B59` | 補足テキスト |
| `--marquee` | `#FFB224` | `#FFB224` | CTA ボタン背景・フォーカスリング・ボール |
| `--marquee-ink` | `#1A1206` | `#1A1206` | アンバー上の文字 |
| `--alert` | `#FF8F8F` | `#B3261E` | エラーメッセージ |

- CTA ボタンは両モードで同一(アンバー地 + 墨文字)。ブランドの錨として明暗で変えない
- ライト/ダークは現行どおり `prefers-color-scheme` 追従を維持(canvas 内テーマ連動も現行実装を活かす)
- コントラスト: `#E4EDE2`/`#0C110D` ≈ 15:1、`#1A1206`/`#FFB224` ≈ 10:1、`#8DA08C`/`#0C110D` ≈ 6.8:1 — いずれも AA 以上

## 2. タイポグラフィ

| ロール | 書体 | 用途 |
|---|---|---|
| ディスプレイ | **DotGothic16**(Google Fonts) | タイトル「草崩し」、リザルト見出し、発射ガイド、CTA ボタン、スコア数字 |
| 本文 | **IBM Plex Sans JP** 400/600 | サブタイトル、入力欄、エラー、統計ラベル、フッター |

- DotGothic16 は日本語ピクセルフォント。かな漢字のドット表示ができる唯一級の Web フォントで、題材(ピクセルグリッド)と直結する
- 読ませる文章には絶対に使わない(1〜2 語の見出し・数字・掛け声のみ)。長文可読性は Plex が担う
- ロード: Google Fonts、`preconnect` + `display=swap`。DotGothic16 は使用グリフが有限(タイトル・見出し・数字・記号)なので `text=` パラメータでサブセット化し数 KB に抑える
- canvas 内 HUD でも DotGothic16 を使うため、セッション開始前に `document.fonts.load()` で確実にロード(未ロード時は sans-serif フォールバックで描画が崩れないこと)

### タイプスケール

- タイトル: `clamp(2rem, 6vw, 3rem)` DotGothic16、字間 `0.08em`
- リザルト見出し: `1.75rem` DotGothic16
- 本文: `0.9375rem / 1.7` Plex 400
- 統計数字: `1.5rem` DotGothic16(ラベルは Plex 0.75rem・`--ink-faint`)

## 3. レイアウト

コンテナ `max-width: 1040px`。1 カラム。ヘッダー/フッターは最小限。

### トップ(アトラクト画面)

```
┌────────────────────────────────────────┐
│              草崩し                      │  ← DotGothic16
│   GitHub の草を、ブロック崩しで刈り取ろう      │  ← Plex, --ink-faint
│                                        │
│ ┌────────────────────────────────────┐ │
│ │ ░▓░ K U S A K U Z U S H I ░▓░      │ │  ← 合成グリッド(3x5ピクセル文字)
│ │ ░ ░░  ▓░   ░▓  ░░  ░  ░░░  ░       │ │     + まばらな level1 ノイズ
│ │              ●                      │ │  ← autopilot ボール(アンバー)
│ │           ▬▬▬▬▬▬                   │ │
│ └────────────────────────────────────┘ │
│                                        │
│   [ GitHub ユーザー名        ] [草を刈る] │  ← 入力 + アンバーCTA
│                                        │
│      ブロック = あなたの1年分の草           │  ← 一行説明
├────────────────────────────────────────┤
│   toshi0607 / Chrome拡張 / 不具合・要望   │  ← フッター小(いずれもリンク)
└────────────────────────────────────────┘
```

- デモ canvas は入力を受け付けない(`pointer-events: none`)。プレイ可能に見えて触れない誤解を防ぐため、盤面下端に小さく `DEMO PLAY` を DotGothic16 で表示
- フォーム送信 → デモを破棄し、同じ場所で本物のセッションに差し替え(レイアウトシフトなし: canvas は同寸)
- ヘッダー(タイトル・サブタイトル)/ ステージ枠 / フッターは **`index.html` の静的マークアップ**。First Paint を JS バンドルのダウンロードと実行から切り離すため(セッション12。JS で組み立てていた間、実オリジンでの FCP/LCP が 4s 台だった)
- ステージの中身(フォーム / ローディング / プレイ / リザルト)は従来どおり JS が入れる。空の状態から中身が入るときにヘッダー・フッターが動かないよう、`.stage` には最初に入るビュー(フォーム)ぶんの高さを `min-height` で先に確保する

### プレイ画面

- 上部に一行ステータス: `@{username} ─ {total} contributions`(Plex、小さく)
- canvas(アスペクト比 **8:3** = 960x360、`width: 100%`)。枠線は `--ridge` 1px のみ。角丸 8px 維持
- 草の帯は正方セル 7 行ぶん = 盤面上端から約 134px(37%)。その直下が HUD 行、残りがパドル空間
- HUD は canvas 内描画(§5)。DOM 側に重複表示しない
- 発射ガイド「クリック / Space で発射」は**草の下端から下(パドル空間)の中央**に置く(`top: 37%` + 中央寄せ = 盤面の約 69%)。盤面全体の中央に置くと草にも HUD 行にも重なる

### リザルト(収穫レポート)

オーバーレイパネル(`--field` 背景 92% + 8px 角丸 + `--ridge` 枠):

```
clear:                              gameOver:
┌──────────────────────────┐        ┌──────────────────────────┐
│      完 全 刈 り 取 り       │ ←状態  │      ゲームオーバー         │ ← DotGothic16 1.75rem
│  地道に積み上げてきた…       │ ←主役  │                          │
│                          │        │   スコア        刈り取り率   │
│   スコア        刈り取り率   │        │   12,340         64%     │
│   12,340         100%    │        │   ▓▓▓▓▓▓▓▓░░░░░░         │
│   ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓        │        │                          │
│  [Xで共有] [画像を保存] [もう一回] │        │  [Xで共有] [画像を保存] [もう一回] │
└──────────────────────────┘        └──────────────────────────┘
   状態: Plex faint 0.6875rem 字間 .22em
   主役: DotGothic16 1.25rem(狭幅 0.875rem)
```

- 刈り取り率バーは「小さい草セル(10px 角丸 2px)の横並び」で描く。プログレスバーですらグリッドの文法で語る
- 主ボタン「Xで共有」のみアンバー、他 2 つは `--field` 地 + `--ridge` 枠のセカンダリ
- **clear では見出しの主役を煽り文に譲る**。「完全刈り取り」はどのブロック崩しでも言える汎用の一言で、この盤面にしか無いのは「消したのは自分の 1 年ぶんだ」と言い当てる側。だから状態名はラベルの体裁(Plex faint・字間広め)に落とし、煽り文を表示書体で大きく組む
  - 見出し要素(`h2`)は状態名のまま。**視覚の主役だけを入れ替えて、読み上げの意味と順序は変えない**
  - 煽り文の書体は DotGothic16。喋っているのはゲーム側であって書き手ではない(本文書体で組むと注釈に見える)
    - **DotGothic16 は `text=` サブセットで読んでいる**(index.html)。サブセットに無い字は**エラーも出さずに Plex へフォールバックする**ので、煽り文を書き換えたら必ず `text=` も足す。煽り文 5 種ぶんで 70 → 126 字 = 4,940 → 8,684 バイト(2026-07-26 実測)。取りこぼしは `apps/web/src/font-subset.test.ts` が落ちて教える
  - 幅は 34em 上限。日本語は語中でも改行できてしまうので、広い画面で「崩れ / 去っていく」のような割れ方をさせない
  - clear のときは 🌱 を付けない。かわいい絵文字の 1 行下で冷たい文が始まると、皮肉が一段弱まる
- **煽り文は clear のときだけ**。壊し残しがある gameOver で「全部消えました」系の文言を出すと嘘になる。gameOver は従来どおり `ゲームオーバー` 一行(構造が clear と違ってよい — 煽る材料が無い状態なので)
- 文言は壊した草の量(その 1 年の総 contributions)で 5 段階に出し分ける(§6)。実装は `packages/core/src/clear-message.ts` の 1 か所で、web と拡張が同じ表を引く。拡張のバナーも同じ主従(状態名 11px faint / 煽り文 14px)にする

### レスポンシブ

- `<560px`: フォーム縦積み(入力とボタンが全幅)、タイトル縮小。リザルトの統計は**横並びのままサイズ縮小**(375px 幅では canvas 高が ~130px しかないため。縦並び案は高さ超過するので不採用 — 2026-07-25 レビュー M3 で確定)。4 ブロック(見出し / 統計 / 刈り取り率バー / ボタン)は**どれも落とさず**、余白・行間・草セル(7px 角・2px 間隔で 10:3 比を維持)を一段小さくして収める。オーバーレイは padding + overflow-y: auto を安全弁に持つ
  - 狭幅は**一段だけ詰める**: `gap` 4→3px、パネル padding 6→5px、スコア 1.05→1rem(2026-07-26 実測。**flex + center 寄せでは `scrollHeight` が溢れを過小報告するので、子の高さ + gap + padding で測る**)
  - それでも clear の 5 ブロックは 146.3px 要り、使える高さ =(幅 − 34)× 0.375 が追いつくのは 424px 以上。そこで **`<560px` ではオーバーレイ = 盤面という縛りを外す**: `bottom: auto` + `min-height: 100%` + `z-index: 1`。足りる幅では盤面ぴったりのまま、足りない幅だけ下へ伸びる(実測のはみ出し: 375px = 18.4px、320px = 39px。gameOver は 4 ブロックなのでどの幅でもはみ出さない)
    - はみ出す先は盤面とパドルレールの間の 8px と、レールの上端(320px でレール 56px のうち 31px)。**ラウンド終了後のレールは無効化済み**(`opacity: .4` + `pointer-events: none`)で誰も触らないので、ここを借りるのが一番実害が小さい
  - 煽り文は狭幅でも 0.875rem を保ち、1 行に収める大きさまでは落とさない(10px 台にすると注釈に逆戻りする)。伸びたぶんはパネルが下へ吸収する
- canvas は全幅縮小のみ(内部解像度 960x360 は不変、現行実装どおり)

### タッチ操作 — パドルレール(`@media (pointer: coarse)`)

主入力がタッチの端末には、盤面の 8px 下に**パドルレール**を出す。盤面上のドラッグだけだと、375px 幅で高さ ~130px しかない盤面を指がボールごと覆い、しかも `pointerdown` が即発射になるため狙えない。

```
┌────────────────────────────────────┐
│ ░▓░ 草 ░░▓ 草 ░▓░░ 草 ░░ 草 ░▓     │
│              ●                      │  ← 盤面(canvas)
│           ▬▬▬▬▬▬                   │
└────────────────────────────────────┘
┌────────────────────────────────────┐
│              ▬▬▬▬                  │  ← ハンドル(--marquee)
│    触れた位置にパドルが動き、離すと発射   │  ← 初回タッチで消える
└────────────────────────────────────┘
```

- レール幅 = canvas の CSS 幅。指の x をそのままパドル中心に写す**絶対マッピング**なので、パドルは指の真下に来る(両端 4.17% だけはパドル中心の可動域 `[w/2, W-w/2]` で頭打ちになる)
- ハンドルは**パドルの影**: 幅は `paddleWidth / canvasWidth`(= 8.33%)、位置は実際のパドル中心に毎フレーム追従。色は `--marquee`(UI 操作系はアンバー、§1 の規則どおり)
- 押している間は移動だけ、**指を離した瞬間に発射**。狙ってから撃てる
- 高さ 56px(iOS HIG の最小タップ領域 44pt 以上)。`touch-action: none` でページスクロールに奪われない
- **レールが出ている端末では、タッチの操作面はレールだけ**。盤面は「`pointerType === "touch"` かつレールが表示中」のとき無視する(マウス/ペンは従来どおり盤面追従)。レールを数 px 外した指が盤面に落ちて「狙う前に発射」に戻るのを防ぐ。判定は**レール自身の幅**(`getBoundingClientRect().width > 0`)で行い、上のメディアクエリを唯一の真実にする — `pointer: coarse` は*主*入力しか見ないので、タッチスクリーン付きノート PC(`pointer: fine` + `any-pointer: coarse`)ではレールが出ない。そこで JS 側も同じ条件で判定しないと、**レールも無い・盤面もタッチを拒む**状態になりタッチ操作が完全に死ぬ
- ドラッグ中は最初の指の `pointerId` だけを見る — 2本目の指がドラッグを奪うと、押したままの親指が効かなくなる。あわせて `setPointerCapture` + `lostpointercapture` で、ウィンドウ外でボタンを離した等で終了イベントが届かないときもレールが握られたままにならないようにする
- ラウンド終了後(gameOver / clear)は盤面の再描画が止まるので、レールも `opacity: .4` + `pointer-events: none` で無効化する。動かない盤面に反応するレールは壊れて見える
- マウス/キーボード環境には出さない(canvas 直操作のほうが速い)。発射ガイドの文言も、タッチ端末では実際に効く面を指して「下のバーで発射」に切り替える(クリックも Space も無い操作を案内しない)

## 4. モーション

演出は**ページロードの 1 シーケンスに集中**、以外はマイクロのみ。

1. **草の生育**(ロード時 / セッション開始時): グリッドのセルが左の週から右へ列単位でスケールイン(列あたり 12ms ずらし、全体 ~700ms、`ease-out`)。GitHub のグラフ読込アニメの記憶とつながる。実装は renderer に `reveal: 0..1` 引数を追加し列ごとにゲート
2. 生育完了後、デモのボールが落ちてきて autopilot 開始
3. マイクロ: ボタン hover(輝度 +6%、120ms)、リザルトパネルは 200ms のフェード + 8px スライドアップ。以上

`prefers-reduced-motion: reduce` 時: 生育アニメは即時全表示、アトラクトのボールは動かさない(静的な草グリッドのみ)、パネルはフェードなし。

## 5. canvas 描画の磨き込み(packages/core/renderer.ts)

現状は矩形ベタ塗り + 16px sans-serif で「グラフに見えない」のが未洗練さの主因。

| 項目 | 現状 | 変更 |
|---|---|---|
| ブロック形状 | 直角矩形 | **正方形** + 角丸(一辺の ~20%、GitHub の 10px セルに 2px 相当)+ **隙間比 22%**(本家 3/13 = 23%)。GitHub のセルは正方形なので、縦長のままでは何を足しても「草グラフ」には見えない。隙間も同様で、詰めると一枚の板になり 1 日 1 セルが読めなくなる |
| ボール | テキスト色の円 | `--marquee` アンバーの円。緑の海の中で唯一の暖色 = 視線誘導 |
| パドル | 矩形 | 角丸カプセル、`--ink` 系 |
| HUD 文字 | `16px sans-serif` | `16px DotGothic16`(Theme に `hudFont?: string` を追加、未指定時は現行フォールバック) |
| HUD 位置 | 盤面上部(草の上に重ねる) | **草の帯の直下**(`layout.brickAreaTop + brickAreaHeight + 10`)の無地の帯。左に `SCORE {n}`、右に `LIFE ●●●`。草を隠さず、背景が無地なので確実に読める。帯の高さは正方セルから決まるので `canvasHeight/2` では代用しない |
| ライフ表示 | `Life: 2` テキスト | **予備のボール**(`--marquee` アンバー、実際のボールと同じ半径)を残機数ぶん並べ、`LIFE` ラベルを添える |
| パーティクル | 現行(緑の欠片) | 維持。数・寿命は現行のまま(足し算しない) |
| 落下アイテム | (新規) | 種類ごとに色を変えた角丸タイル + 背景色で抜いたピクセル記号。玉増加=**青**(light `#0969da` / dark `#58a6ff`)+ 三角に並んだ3点、バー増加=**紫**(light `#8250df` / dark `#a371f7`)+ 横並びの3本(手に入るものそのままの形)。どちらも GitHub のアクセント色で、緑=草・アンバー=玉/UI に対する第3・第4の色。記号が読める距離まで落ちてきた頃にはもうパドル直前なので、追うかどうかは色だけで判断できる必要がある(`Theme.itemColors`) |

Theme 型の変更は optional フィールドのみ(`hudFont?`, `accentColor?`, `itemColors?`)。拡張版・既存テストへの後方互換を壊さない。

## 6. コピー(文言)

| 箇所 | 文言 |
|---|---|
| タイトル | 草崩し |
| サブタイトル | GitHub の草を、ブロック崩しで刈り取ろう |
| 入力 placeholder | GitHub ユーザー名 |
| CTA | 草を刈る |
| ローディング | {user} の草を取得中… |
| 発射ガイド | クリック / Space で発射 |
| クリアの状態名 | 完全刈り取り(ラベル扱い。🌱 は付けない) |
| クリアの煽り文 | 壊した草の量で 5 段階(下表)。パネルの主役 |
| ゲームオーバー見出し | ゲームオーバー |
| 統計ラベル | スコア / 刈り取り率 |
| 草ゼロ | 崩す草がありません🌵(現行維持) |
| エラー | 現行維持(ユーザー不在 / 取得失敗) |
| デモ表示 | DEMO PLAY |

方針: 操作を言い切る動詞(「草を刈る」= このプロダクトの中心動詞)。飾り言葉なし。絵文字は既存の 🌱🌵 の 2 つ以外増やさない。

### クリアの煽り文(壊した草の量で出し分け)

その 1 年の総 contributions で段を決める。**祝わない** — 盤面を空にすることは 1 年ぶんの記録を消すことなので、消した量が多いほど文言は冷たくなる。実装は `packages/core/src/clear-message.ts`(web / 拡張が同じ表を引く唯一の場所)。

| 総 contributions | 文言 |
|---|---|
| 3,000 〜 | 地道に積み上げてきたものが崩れ去っていく気分はいかがですか? |
| 1,500 〜 2,999 | 1年ぶんの精進が、まとめて更地になりました。 |
| 500 〜 1,499 | 毎日コツコツ積み上げた緑が、きれいに消えました。 |
| 100 〜 499 | ひと通り刈り終えました。跡形もありません。 |
| 〜 99 | この量なら、来週にはもう生え揃っていますよ。 |

- ここだけは飾り言葉を許す(このゲームの落ちそのものなので)。ただし**絵文字は足さない**、**1 文**、**最長 30 字**(共有画像の 1 行に収まる上限でもある)
- 拡張側は総数を GitHub の見出し(`#js-contribution-activity-description`)から読む。`grid.total` は `level²` の合計でスコア用の重みなので段位には使えない。見出しが読めなければ**何も出さない**(適当な段を当てない)
- **煽り文は共有物にも載せる**(§8)。落ちが本人の画面に数秒出るだけで、拡散する絵には残らないのでは意味がない
- 文言を変えたら、表示に使う**フォントのサブセットを 2 か所**足すこと: `apps/web/index.html` の `text=`(2 リンク)と `workers/ogp/src/fonts.ts`。どちらも取りこぼしは無言のフォールバック(前者は Plex、後者は豆腐)になる — テストが両方を見張っている

## 7. アトラクトモード仕様(apps/web)

- `demo-grid.ts`: `buildDemoGrid(): ContributionGrid`。53x7 のグリッドに 3x5 ピクセル文字で `KUSAKUZUSHI`(11 文字 × 4 列 = 43 列、上下 1 行マージン)を **level 4 のベタ**で描き、残りセルに疎な level 1 ノイズ(密度 ~12%)を撒く
  - 文字は単一レベル。濃淡を混ぜると、セル間に本家と同じ 22% の隙間が入った時点で 3 セル幅のストロークがディザに見えて字形が崩れる(gitfiti の実物も単一レベルのベタ描き)。ワードマークはこの画面のシグネチャーなので、可読性が濃淡の面白さに優先する
- autopilot: 専用モジュール(attract.ts)。パドル目標 x = ボール x + `18 * sin(t)`(完璧すぎない追従)、最大速度は人間相当にクランプ。ballLost → 1.2 秒後に自動再発射。gameOver / clear → グリッド再生成して最初から(**ライフは core の機構のまま**。ライフ無限案は core 無変更の制約を優先して不採用 — アーケードのデモが定期的に頭へ戻るのは自然な挙動。2026-07-25 レビュー L3 で確定)
- HUD はデモ中非表示(スコアの意味がないため)。`DEMO PLAY` のみ右下に表示
- フォーム送信時に demo セッションを destroy → 実セッションを同一コンテナへ

## 8. 共有画像(リザルト保存)の体裁

現状はゲーム canvas の生スクショ。共有される絵がプロダクトの顔になるため、オフスクリーン canvas 1200x630(OGP 比)に合成する:

- 背景 `--soil`、最終盤面のスナップショット(角丸クリップ)、`@{username}`、スコア/刈り取り率(DotGothic16)、右下にワードマーク「草崩し」
- **クリア回の煽り文は「盤面の中」に置く**。クリア = 全ブロック破壊なので、スナップショットは 6 割が空白になる。その空白こそ「更地になった」という絵なので、文をそこに置けば**草があった場所を文が占める**。34px で最長 30 字 = 1020px、盤面幅 1080px に左右 30px 残して 1 行(中央揃え、盤面高の 56% = 残骸の行とパドルの間)
- **カード用の盤面は HUD 無しで描き直す**(`render(..., { hud: false })`)。画面の canvas をそのまま渡すと `SCORE` / `LIFE` が焼き込まれ、下のキャプションと同じ数字が 2 回出る
- キャプションは `@{username} ― 完全刈り取り`(24px faint)→ `スコア … / 刈り取り率 …`(40px)+ 右にワードマーク。gameOver 回は盤面にブロックが残るので、煽り文なしで同じキャプションだけ
- canvas は**未読込のグリフを黙って別書体で描く**ので、合成前に `document.fonts.load()` でカードが描く文字を要求する。要求しないと煽り文だけ丸ゴシックで焼き付く(2026-07-26 実物で確認)
- OGP Worker のカード(`/share/{user}/og.png`)も、`p=100`(= clear。刈り取り率は floor なので 1 ブロックでも残れば 99% 以下)のときは**保存画像と同じことを言う**:
  - **グリッドを全 level 0 で描く**(`buildContributionGridSvg(weeks, { emptied: true })`)。生きた緑の草の下に「100% 刈り取った」と書くと、絵と文が矛盾する。更地のグリッドは暗いセルの整列として残るので、空カードには見えない(2026-07-26 実レンダリングで確認)
  - **主従もアプリと揃える**: `{user} の草を {p}% 刈り取った` を 26px・不透明度 0.7 に落とし、煽り文 34px を主役に、スコアは 30px。gameOver 回は従来どおり 48px / 40px
  - 総数はカード用に取得しているグリッドから合計する。satori 側のフォントも `text=` サブセットなので `fonts.ts` に文字を足すこと(足し忘れは全部豆腐になる)
- X intent のテキストは現行仕様を維持。煽り文は**ゲームがプレイヤーに言う二人称**なので、本人の投稿本文に置くと主体がねじれる。画像の中(= ゲームの声)に置く

## 9. 品質フロア(全フェーズ共通の完了条件)

- モバイル(375px)〜デスクトップで破綻なし
- フォーカスリング: `outline: 2px solid var(--marquee); outline-offset: 2px` を全操作要素に
- `prefers-reduced-motion` 尊重(§4)
- ライト/ダーク両モードでスクリーンショット確認
- Lighthouse で FOUT/CLS の劣化なし(フォント導入後に確認)

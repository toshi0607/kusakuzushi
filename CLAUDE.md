# CLAUDE.md

このリポジトリで作業する Claude Code セッションへの指示。

## TypeScript バージョン方針: 新規は最初から TS7

このリポジトリは TypeScript 7(Go 実装)を採用済み(2026-07-26、PR #43。経緯は
`tasks/todo.md` の「TypeScript 7 の扱い」)。全パッケージ `^7.0.2`。

**新しいパッケージ・アプリ・Worker を追加するときは、最初から TypeScript 7 で始める。**
TS5/TS6 で作ってからメジャーアップグレードする、という順序は踏まない。

- npm の `latest` はすでに 7 系(2026-07-26 時点で 7.0.2。`npm view typescript dist-tags`
  で確認できる)。素の `pnpm add -D typescript` で 7 が入る。
- ただしスキャフォールダ(`create-vite` などのテンプレート)は古いメジャー(5/6 系)を
  pin してくることがある。**scaffold 直後・コードを書き始める前に** `package.json` の
  `typescript` を `^7` に上げ、`tsc --noEmit` が通ることを確認する。コードがまだ無い
  時点で上げれば、破壊的変更への対応コストは実質ゼロになる。
- 型検査は `tsc --noEmit` 専用、バンドルは vite、という分離を新規パッケージでも保つ。
  この構造なら型検査の実装が変わっても成果物に影響しない(既存 4 パッケージで実証済み)。
- TypeScript のコンパイラ API に依存するツール(typescript-eslint、ts-jest、エディタ
  プラグイン等)を足すときは、TS7(ネイティブ実装)対応のバージョンを選ぶ。

### リポジトリ横断について

この方針はこのリポジトリに限らない意図のもの。ただしリモートセッションが確実に読むのは
各リポジトリの CLAUDE.md なので、他のリポジトリで新規に TS を使うときは、この節を
そのリポジトリの CLAUDE.md にも置くこと(このファイルをコピーの起点にしてよい)。
